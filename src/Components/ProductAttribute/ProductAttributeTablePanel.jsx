import Thead from "../Table/Thead";
import Table from "../Table/Table";
import { ProductAttributeTableRow } from "./ProductAttributeTableRow";

export const ProductAttributeTablePanel = ({ editMode, editItem, reload, data }) => {
    return (
        <>
            <Table>
                <Thead heads={[
                    { title: "شناسه" },
                    { title: "محصول" },
                    { title: "نام ویژگی" },
                    { title: "ویرایش" },
                    { title: "حذف" },

                ]} />
                <tbody>
                    {
                        data && data.map((item, index) => (
                            <ProductAttributeTableRow editMode={editMode} editItem={editItem} reload={reload} item={item} />
                        ))
                    }
                </tbody>
            </Table>
        </>
    )
}
