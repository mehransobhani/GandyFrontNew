import Thead from "../Table/Thead";
import Table from "../Table/Table";
import { AttributeSelectTableRow } from "./AttributeSelectTableRow";

export const AttributeSelectTablePanel = ({ editMode, editItem, reload, data }) => {
    return (
        <>
            <Table>
                <Thead heads={[
                    { title: "شناسه" },
                    { title: "نام ویژگی" },
                    { title: "مقدار ویژگی" },
                    { title: "ویرایش" },
                    { title: "حذف" },

                ]} />
                <tbody>
                    {
                        data && data.map((item, index) => (
                            <AttributeSelectTableRow editMode={editMode} editItem={editItem} reload={reload} item={item} />
                        ))
                    }
                </tbody>
            </Table>
        </>
    )
}
